"""Une bibliothèque exporte ses titres vers un fichier binaire.

Implémente `to_utf8(title: str) -> bytes` :
- renvoie la représentation UTF-8 du titre,
- type de retour : bytes.
"""


def to_utf8(title: str) -> bytes:
    ...
