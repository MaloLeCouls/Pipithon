"""Un site e-commerce veut la liste des prix dépassant un seuil.

Implémente `expensive_prices(prices: list[int], threshold: int) -> list[int]` :
- renvoie les prix strictement supérieurs à `threshold`,
- dans l'ordre d'origine,
- via une list comprehension (pas de boucle for + append).
"""


def expensive_prices(prices: list[int], threshold: int) -> list[int]:
    ...
