"""Un restaurant compare les allergènes de deux plats (listes de str).

Implémente :

1. `common_allergens(a: list[str], b: list[str]) -> set[str]`
   les allergènes présents dans les DEUX plats.

2. `only_in_first(a: list[str], b: list[str]) -> set[str]`
   les allergènes du plat `a` absents de `b`.

Utilise les opérations ensemblistes (pas de boucles imbriquées).
"""


def common_allergens(a: list[str], b: list[str]) -> set[str]:
    ...


def only_in_first(a: list[str], b: list[str]) -> set[str]:
    ...
