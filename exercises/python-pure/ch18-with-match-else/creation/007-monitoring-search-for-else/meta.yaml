id: ch18-007-monitoring-search-for-else
chapter: 18
chapter_slug: with-match-else
type: creation
difficulty: 3
estimated_minutes: 9
concepts:
  - for-else
  - search-pattern
theme: monitoring
title: "for/else : find-or-default sans drapeau"
short_description: "Le `else` d'un for s'exécute *quand le for ne break pas*."
hints:
  - "`for ... : ... break` -> ne passe PAS dans le else."
  - "Sans break (boucle complète) -> passe dans le else. C'est la sémantique inversée."
  - "Le bloc else remplace un `found = False` + `if not found:` à la fin."
reference_book: "Fluent Python ch 18 — for/else / else on loops"
pymistral_link: null
tags:
  - for-else
  - control-flow
