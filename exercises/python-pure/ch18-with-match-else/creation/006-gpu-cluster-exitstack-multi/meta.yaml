id: ch18-006-gpu-cluster-exitstack-multi
chapter: 18
chapter_slug: with-match-else
type: creation
difficulty: 2
estimated_minutes: 12
concepts:
  - exit-stack
  - dynamic-context
theme: gpu-cluster
title: "ExitStack : verrouiller N GPUs en un seul with"
short_description: "Empiler dynamiquement des context managers — sortie LIFO garantie."
hints:
  - "`from contextlib import ExitStack`."
  - "`with ExitStack() as stack: for gpu in gpus: stack.enter_context(GPULock(gpu))`."
  - "À la sortie, ExitStack appelle `__exit__` de chacun en ordre inverse — même si exception."
reference_book: "Fluent Python ch 18 — ExitStack for dynamic context managers"
pymistral_link: null
tags:
  - context-manager
  - exit-stack
