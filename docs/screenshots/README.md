# Captures pour le README

4 PNG, un par niveau de navigation. Thème sombre (le seul que l'app propose).
Garde un exo réussi au moins une fois pour que badges et progression soient
parlants.

| Fichier | Contenu attendu |
|---|---|
| `home.png` | Page racine `/`, liste des tracks. `python-pure` actif avec compteur d'exos. |
| `track.png` | Page track `/tracks/python-pure`, liste des chapitres + progression globale. |
| `chapter.png` | Page chapitre `/tracks/python-pure/ch01-data-model` (ou autre bien peuplé). Met en avant le mélange `creation`/`modification`/`debugging`. |
| `workbench.png` | Page exo. Idéalement après un Submit vert : panneau gauche énoncé + indices, droit Monaco + pytest tout verts. Confetti = capte juste après. |
